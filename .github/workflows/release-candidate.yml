name: Release candidate evidence

on:
  workflow_dispatch:
    inputs:
      base_url:
        description: Deployed HTTPS origin to verify
        required: true
        default: https://cafe1stalbans.co.uk
        type: string
      database_check_run_url:
        description: Successful Production checks workflow URL containing the database job
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: cafe1-release-candidate
  cancel-in-progress: false

jobs:
  evidence:
    name: Build, audit, smoke and evidence
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Check out repository
        uses: actions/checkout@v7.0.1

      - name: Use Node.js 22
        uses: actions/setup-node@v7.0.0
        with:
          node-version: 22
          cache: npm

      - name: Clean install
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Verify, test and build release tree
        run: npm run release:guard && npm run check

      - name: Verify generated route coverage
        run: npm run verify:routes

      - name: Audit production dependencies
        run: mkdir -p release-evidence && npm audit --omit=dev --audit-level=high --json > release-evidence/npm-audit.json

      - name: Generate CycloneDX SBOM
        run: npm sbom --sbom-format cyclonedx > release-evidence/sbom.cdx.json

      - name: Verify deployed production
        env:
          PRODUCTION_BASE_URL: ${{ inputs.base_url }}
          EXPECTED_RELEASE_SHA: ${{ github.sha }}
        run: set -o pipefail; npm run smoke:production -- --json release-evidence/production-smoke.json 2>&1 | tee release-evidence/production-smoke.log

      - name: Run deployed desktop and mobile journeys
        env:
          E2E_BASE_URL: ${{ inputs.base_url }}
        run: npm run test:e2e

      - name: Record repository status
        if: always()
        run: |
          npm run verify:dependencies -- --output release-evidence/dependency-contract.json
          npm run verify:migrations -- --output release-evidence/migration-integrity.json
          npm run verify:capabilities -- --output release-evidence/software-capabilities.json
          npm run verify:bundle-budget -- --output release-evidence/bundle-budget.json
          npm run release:status -- --output release-evidence/release-status.json

      - name: Write evidence manifest
        if: always()
        env:
          PRODUCTION_BASE_URL: ${{ inputs.base_url }}
          DATABASE_CHECK_RUN_URL: ${{ inputs.database_check_run_url }}
        run: node scripts/write-release-evidence.mjs

      - name: Upload release evidence
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: cafe1-release-evidence-${{ github.sha }}
          path: |
            release-evidence/
            test-results/playwright/
          if-no-files-found: error
          retention-days: 90
